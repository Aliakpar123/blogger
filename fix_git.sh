#!/bin/bash
echo "🔧 Начинаем починку связи с GitHub..."

# 1. Инициализация если нет
git init

# 2. Удаляем старую связь если была (чтобы не было ошибок)
git remote remove origin 2>/dev/null

# 3. Добавляем правильную ссылку (из вашего скриншота)
git remote add origin https://github.com/Aliakpar123/blogger.git

# 4. Собираем файлы
git branch -M main
git add .
git commit -m "Fix Version 5.0 Force Update"

# 5. Отправляем силой (перезаписываем GitHub вашей версией)
echo "🚀 Отправляем файлы..."
git push -u origin main --force

echo "✅ Готово! Если не было красных ошибок выше - сайт обновится."
