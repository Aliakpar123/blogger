# Инструкция по запуску через Vercel

Vercel — это самый быстрый способ сделать ваш сайт доступным по ссылке (HTTPS), что нужно для Telegram.

## Способ 1: Через Терминал (Самый быстрый) ⚡️

Если у вас установлен Node.js, это займет 1 минуту.

1. Откройте **Терминал** на Mac (`Command + Пробел`, наберите `Terminal`).
2. Скопируйте и вставьте эту команду, чтобы перейти в папку проекта:
   ```zsh
   cd "/Users/aliakbar/Вишт лист для блоггеров "
   ```
3. Запустите деплой одной командой:
   ```zsh
   npx vercel
   ```
   *(Если спросит установку, нажмите `y` и Enter)*

4. **Отвечайте на вопросы:**
   - **Log in:** Вас перекинет в браузер, войдите через GitHub или Email.
   - **Set up and deploy?** → Нажмите `Y` (Yes).
   - **Which scope?** → Нажмите `Enter` (выберите свой аккаунт).
   - **Link to existing project?** → Нажмите `N` (No).
   - **Project name?** → Нажмите `Enter` (оставьте как есть или напишите `wish-list`).
   - **In which directory?** → Нажмите `Enter` (оставьте `./`).
   - **Want to modify settings?** → Нажмите `N` (No).

5. **Готово!** Терминал выдаст ссылку `Production: https://...`. Скопируйте её.

---

## Способ 2: Через сайт (Если терминал не работает) 🌐

1. Зайдите на [vercel.com](https://vercel.com) и войдите (Sign Up / Login).
2. Нажмите кнопку **"Add New"** → **"Project"**.
3. **Вариант с GitHub** (если вы уже залили код на GitHub):
   - Слева в списке вы увидите свой репозиторий. Нажмите кнопку **Import**.
4. **Вариант без GitHub** (Drag & Drop):
   - Установите Vercel CLI командой `npm i -g vercel` (требует Терминал) ИЛИ:
   - Просто залейте код на GitHub (см. инструкцию по Mac), и Vercel увидит его автоматически.

*Примечание: Прямой загрузки файлов "перетаскиванием" на сайте Vercel в 2024 году нет, они требуют Git или CLI. Поэтому Способ 1 (CLI) обычно проще.*

## Что делать с ссылкой?
Полученную ссылку (например `https://wish-list-aliakbar.vercel.app`) нужно отправить **BotFather** в Telegram, когда он спросит **Web App URL**.
